<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fitlinkv3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitLinkv3"
        tools:replace="android:allowBackup">
        <activity android:name=".Settings"></activity>
        <activity android:name=".FAQs" />
        <activity android:name=".StravaAuth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChatBot" />
        <!-- <activity -->
        <!-- android:name=".Database.AddingShat" Dont know why -->
        <activity android:name=".ProfilePage" />
        <activity android:name=".NutritionPage" />
        <activity android:name=".ProgressPage" />
        <activity android:name=".ExplorePage" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>